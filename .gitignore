*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build/
*.jks

# Signing secrets — never commit
keystore.properties

# Built artifacts — distribute via Releases, not git
*.apk
