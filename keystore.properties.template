# Copy this file to keystore.properties (which is gitignored) and fill in real
# values. Release builds read these to sign the APK. Never commit the real file.
# Alternatively, set KAIROS_STORE_FILE / KAIROS_STORE_PASSWORD /
# KAIROS_KEY_ALIAS / KAIROS_KEY_PASSWORD as environment variables (e.g. in CI).
storeFile=../kairostimer_release.jks
storePassword=CHANGE_ME
keyAlias=kairostimer
keyPassword=CHANGE_ME
